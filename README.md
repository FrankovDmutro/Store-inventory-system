# 🏪 Система управління інвентарем магазину (Store Inventory System)

## 📋 Зміст
- [Опис проєкту](#опис-проєкту)
- [Основні функції](#основні-функції)
- [Технології](#технології)
- [Структура бази даних](#структура-бази-даних)
- [Ролі користувачів](#ролі-користувачів)
- [Встановлення та налаштування](#встановлення-та-налаштування)
- [Використання](#використання)
- [Структура проєкту](#структура-проєкту)

---

## 📖 Опис проєкту

**Store Inventory System** — це веб-додаток на Django для управління магазином роздрібної торгівлі. Система забезпечує повний цикл управління товарами, від закупівлі до продажу, з детальною аналітикою та звітністю.

### Цільове призначення:
- Автоматизація роботи касирів на точці продажу (POS)
- Управління складськими залишками
- Облік постачань та взаємодія з постачальниками
- Облік списань та повернень товарів
- Аналітика продажів та прибутковості
- Контроль термінів придатності

---

## ✨ Основні функції

### Для касирів:
- 💳 **POS-система** — швидке оформлення продажів з інтуїтивним інтерфейсом
- 🛒 Робота з кошиком покупок
- 📱 Перегляд чеків та історії продажів
- 📄 Генерація PDF-чеків для роздруку
- 🔍 Пошук товарів за категоріями

### Для менеджерів:
- 📊 **Дашборд з аналітикою** — статистика продажів, прибутків, популярних товарів
- 📦 Управління товарами (CRUD операції)
- 🏢 Управління постачальниками
- 📥 Облік закупівель та поставок
- ❌ Списання товарів (бій, брак, термін придатності)
- 🔄 Обробка повернень від клієнтів
- ⚠️ Моніторинг товарів з близьким терміном придатності
- 📈 Детальна звітність та статистика

---

## 🛠 Технології

### Backend:
- **Python 3.x**
- **Django 5.2.9** — основний фреймворк
- **MySQL** — база даних
- **Pillow** — обробка зображень
- **ReportLab** — генерація PDF

### Frontend:
- **Bootstrap 5** — адаптивний дизайн
- **crispy-forms** — стилізовані форми
- **JavaScript/jQuery** — інтерактивність
- **HTMX** — динамічне оновлення контенту

### Інші інструменти:
- **python-dotenv** — управління змінними середовища
- **Faker** — генерація тестових даних
- **django-humanize** — форматування чисел та дат

---

## 🗄 Структура бази даних

### 1️⃣ **Supplier (Постачальники)**

Зберігає інформацію про постачальників товарів.

| Колонка | Тип | Опис |
|---------|-----|------|
| `id` | Integer (PK) | Унікальний ідентифікатор |
| `name` | CharField(200) | Назва постачальника |
| `email` | EmailField | Електронна пошта |
| `phone` | CharField(30) | Телефон |
| `address` | CharField(255) | Адреса |
| `notes` | TextField | Примітки |
| `categories` | ManyToMany | Зв'язок з категоріями товарів |
| `created_at` | DateTime | Дата створення запису |

---

### 2️⃣ **Category (Категорії товарів)**

Категорії для організації товарів.

| Колонка | Тип | Опис |
|---------|-----|------|
| `id` | Integer (PK) | Унікальний ідентифікатор |
| `name` | CharField(100) | Назва категорії |
| `image` | ImageField | Зображення категорії |

**Приклади категорій:** Молочні продукти, Напої, Хлібобулочні вироби, Консерви тощо.

---

### 3️⃣ **Product (Товари)**

Основна таблиця товарів із повною інформацією про продукцію.

| Колонка | Тип | Опис |
|---------|-----|------|
| `id` | Integer (PK) | Унікальний ідентифікатор |
| `sku` | CharField(20) | Артикул/код товару |
| `name` | CharField(200) | Назва товару |
| `category` | ForeignKey | Зв'язок з категорією |
| `supplier` | ForeignKey | Зв'язок з постачальником |
| `description` | TextField | Опис товару |
| `weight_value` | Decimal(6,3) | Вага/об'єм товару |
| `weight_unit` | CharField(10) | Одиниця виміру (шт, кг, г, л, мл) |
| `purchase_price` | Decimal(10,2) | Ціна закупівлі (грн) |
| `price` | Decimal(10,2) | Ціна продажу (грн) |
| `quantity` | PositiveInteger | Кількість на складі |
| `expiry_date` | DateField | Термін придатності |
| `image` | ImageField | Фото товару |
| `created_at` | DateTime | Дата додавання |

**Валідація:**
- Ціна продажу не може бути меншою за ціну закупівлі
- Кількість не може бути від'ємною
- Ціни повинні бути більше нуля

**Методи:**
- `margin()` — обчислює прибуток з одиниці товару (ціна продажу - ціна закупівлі)

---

### 4️⃣ **Purchase (Поставки)**

Облік закупівель товарів від постачальників.

| Колонка | Тип | Опис |
|---------|-----|------|
| `id` | Integer (PK) | Унікальний ідентифікатор |
| `supplier` | ForeignKey | Постачальник |
| `status` | CharField(20) | Статус (чернетка/замовлено/отримано/скасовано) |
| `expected_date` | DateTime | Очікувана дата поставки |
| `total_cost` | Decimal(12,2) | Загальна вартість поставки |
| `received_applied` | Boolean | Чи проведено на склад |
| `created_at` | DateTime | Дата створення |

**Статуси поставок:**
- `draft` — Чернетка
- `ordered` — Замовлено
- `received` — Отримано
- `cancelled` — Скасовано

**Методи:**
- `recalc_total()` — перераховує загальну вартість поставки
- `apply_to_stock_once()` — додає товари на склад (після отримання)

---

### 5️⃣ **PurchaseItem (Позиції поставки)**

Окремі товари в поставці.

| Колонка | Тип | Опис |
|---------|-----|------|
| `id` | Integer (PK) | Унікальний ідентифікатор |
| `purchase` | ForeignKey | Зв'язок з поставкою |
| `product` | ForeignKey | Товар |
| `quantity` | PositiveInteger | Кількість |
| `unit_cost` | Decimal(10,2) | Ціна за одиницю |

**Методи:**
- `line_total()` — обчислює вартість позиції (кількість × ціна)

---

### 6️⃣ **Order (Чеки/Замовлення)**

Головна таблиця продажів (шапка чека).

| Колонка | Тип | Опис |
|---------|-----|------|
| `id` | Integer (PK) | Номер чека |
| `created_at` | DateTime | Дата та час створення |
| `total_price` | Decimal(10,2) | Загальна сума чека |
| `total_profit` | Decimal(10,2) | Загальний прибуток з чека |

**Індекси:** `created_at` — для швидкого пошуку за датою

---

### 7️⃣ **OrderItem (Позиції чека)**

Товари в чеку.

| Колонка | Тип | Опис |
|---------|-----|------|
| `id` | Integer (PK) | Унікальний ідентифікатор |
| `order` | ForeignKey | Зв'язок з чеком |
| `product` | ForeignKey | Товар |
| `quantity` | PositiveInteger | Кількість проданих одиниць |
| `price` | Decimal(10,2) | Ціна продажу за одиницю |
| `purchase_price` | Decimal(10,2) | Собівартість (на момент продажу) |

**Методи:**
- `get_cost()` / `get_line_total()` — сума за позицію (ціна × кількість)
- `get_profit()` — прибуток за позицію ((ціна продажу - собівартість) × кількість)

---

### 8️⃣ **WriteOff (Списання)**

Облік списаних товарів.

| Колонка | Тип | Опис |
|---------|-----|------|
| `id` | Integer (PK) | Унікальний ідентифікатор |
| `product` | ForeignKey | Товар |
| `quantity` | PositiveInteger | Кількість списаного товару |
| `reason` | CharField(20) | Причина списання |
| `comment` | TextField | Коментар |
| `manager` | ForeignKey(User) | Менеджер, що списав |
| `purchase_price` | Decimal(10,2) | Собівартість на момент списання |
| `created_at` | DateTime | Дата списання |

**Причини списання:**
- `damage` — Бій/Пошкодження
- `defect` — Брак
- `expiry` — Термін придатності
- `other` — Інше

**Методи:**
- `get_total_loss()` — обчислює загальну суму збитків від списання

---

### 9️⃣ **Return (Повернення)**

Облік повернень товарів від клієнтів.

| Колонка | Тип | Опис |
|---------|-----|------|
| `id` | Integer (PK) | Унікальний ідентифікатор |
| `order` | ForeignKey | Зв'язок з оригінальним чеком |
| `reason` | CharField(20) | Причина повернення |
| `comment` | TextField | Коментар |
| `processed_by` | ForeignKey(User) | Касир, що обробив повернення |
| `created_at` | DateTime | Дата повернення |

**Причини повернення:**
- `defective` — Бракований товар
- `wrong_product` — Не той товар
- `changed_mind` — Передумав покупець
- `expired` — Прострочений товар
- `other` — Інше

**Методи:**
- `get_total_refund()` — загальна сума повернення
- `get_total_loss()` — втрачений прибуток

---

### 🔟 **ReturnItem (Позиції повернення)**

Окремі товари в поверненні.

| Колонка | Тип | Опис |
|---------|-----|------|
| `id` | Integer (PK) | Унікальний ідентифікатор |
| `return_instance` | ForeignKey | Зв'язок з поверненням |
| `product` | ForeignKey | Товар |
| `quantity` | PositiveInteger | Кількість повернутого товару |
| `unit_price` | Decimal(10,2) | Ціна продажу за одиницю |
| `purchase_price` | Decimal(10,2) | Собівартість за одиницю |

**Методи:**
- `get_line_total()` — сума повернення за позицію
- `get_line_loss()` — збиток за позицію

---

## 📊 Діаграма зв'язків таблиць

```
Supplier ──┬── Purchase ─── PurchaseItem ─── Product
           │                                    │
           └────────────────────────────────────┤
                                                │
Category ──────────────────────────────────────┤
                                                │
                                          ┌─────┴─────┐
                                          │           │
Order ─── OrderItem ───────────────────── │      WriteOff
  │                                       │
  └─── Return ─── ReturnItem ─────────────┘

User (Django Auth)
  ├── manager (WriteOff)
  └── processed_by (Return)
```

---

## 👥 Ролі користувачів

Система використовує вбудовану систему груп Django для розмежування прав доступу:

### 1. **Cashiers (Касири)**
- Доступ до POS-екрану (точка продажу)
- Оформлення продажів
- Перегляд власних чеків
- Додавання товарів до кошика
- Генерація PDF-чеків

### 2. **Managers (Менеджери)**
- Усі функції касира +
- Повний доступ до дашборду з аналітикою
- Управління товарами (додавання, редагування, видалення)
- Управління постачальниками
- Створення та обробка закупівель
- Списання товарів
- Обробка повернень
- Перегляд усіх чеків та статистики
- Моніторинг товарів з терміном придатності

**Налаштування ролей:**
```bash
python manage.py setup_roles
```

---

## 🚀 Встановлення та налаштування

### Передумови:
- Python 3.8+
- MySQL 5.7+ або 8.0+
- pip (менеджер пакетів Python)

### Крок 1: Клонування репозиторію
```bash
git clone <URL-репозиторія>
cd Store-inventory-system
```

### Крок 2: Створення віртуального середовища
```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate
```

### Крок 3: Встановлення залежностей
```bash
pip install -r requirements.txt
```

### Крок 4: Налаштування бази даних

Створіть файл `.env` у кореневій директорії проєкту:

```env
# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DB_NAME=store_inventory_db
DB_USER=root
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=3306
```

### Крок 5: Створення бази даних MySQL
```sql
CREATE DATABASE store_inventory_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Крок 6: Міграції бази даних
```bash
python manage.py makemigrations
python manage.py migrate
```

### Крок 7: Створення суперкористувача
```bash
python manage.py createsuperuser
```

### Крок 8: Налаштування груп та ролей
```bash
python manage.py setup_roles
```

### Крок 9: Завантаження тестових даних (опціонально)
```bash
# Завантажити постачальників
python manage.py seed_suppliers

# Завантажити товари
python manage.py seed_products

# Завантажити демо-дані (чеки, замовлення)
python manage.py seed_demo_data

# АБО завантажити все одразу:
python manage.py seed_all
```

### Крок 10: Запуск сервера
```bash
python manage.py runserver
```

Відкрийте браузер: `http://127.0.0.1:8000/`

---

## 📱 Використання

### Вхід в систему

1. Перейдіть на головну сторінку
2. Введіть логін та пароль
3. Система автоматично перенаправить:
   - **Касирів** → POS-екран
   - **Менеджерів** → Дашборд менеджера

### Робота касира

#### Оформлення продажу:
1. Виберіть категорію товарів
2. Натисніть "Додати до кошика" на потрібних товарах
3. Перегляньте кошик праворуч
4. Натисніть "Оформити замовлення"
5. Підтвердіть продаж
6. Завантажте PDF-чек для роздруку

#### Перегляд історії:
- Меню → "Мої чеки"
- Пошук за датою або номером чека

### Робота менеджера

#### Дашборд:
- Загальна статистика: виручка, прибуток, кількість чеків
- Топ-10 найпопулярніших товарів
- Графіки продажів за період
- Товари з низьким залишком

#### Управління товарами:
1. Меню → "Товари"
2. Додати/Редагувати/Видалити товар
3. Пошук та фільтрація за категоріями

#### Закупівлі:
1. Меню → "Постачальники"
2. Створити нову поставку
3. Додати товари до поставки
4. Вказати ціни та кількість
5. Змінити статус на "Отримано" → товари автоматично додаються на склад

#### Списання товарів:
1. Меню → "Списання"
2. Вибрати товар та кількість
3. Вказати причину (бій, брак, термін придатності)
4. Підтвердити списання

#### Моніторинг термінів придатності:
1. Меню → "Прострочені товари"
2. Перегляд товарів з близьким терміном придатності
3. Швидке створення списання

---

## 📁 Структура проєкту

```
Store-inventory-system/
│
├── manage.py                      # Django CLI
├── requirements.txt               # Залежності Python
├── .env                          # Змінні середовища (НЕ в Git!)
│
├── shop_core/                    # Основні налаштування Django
│   ├── settings.py               # Конфігурація проєкту
│   ├── urls.py                   # Головні URL-маршрути
│   ├── wsgi.py                   # WSGI точка входу
│   └── asgi.py                   # ASGI точка входу
│
├── store/                        # Основний додаток
│   ├── models.py                 # Моделі бази даних
│   ├── views.py                  # Логіка представлень
│   ├── urls.py                   # URL-маршрути додатку
│   ├── forms.py                  # Django форми
│   ├── services.py               # Бізнес-логіка (сервіси)
│   ├── utils.py                  # Допоміжні функції
│   ├── middleware.py             # Middleware обробники
│   ├── admin.py                  # Налаштування адмін-панелі
│   │
│   ├── management/commands/      # Власні команди manage.py
│   │   ├── setup_roles.py        # Налаштування ролей
│   │   ├── seed_suppliers.py     # Генерація постачальників
│   │   ├── seed_products.py      # Генерація товарів
│   │   ├── seed_demo_data.py     # Генерація демо-даних
│   │   └── seed_all.py           # Генерація всіх даних
│   │
│   ├── migrations/               # Міграції бази даних
│   │
│   ├── templates/store/          # HTML-шаблони
│   │   ├── base.html             # Базовий шаблон
│   │   ├── category_list.html    # Список категорій
│   │   ├── pos_screen.html       # POS-екран
│   │   ├── manager_dashboard.html # Дашборд менеджера
│   │   ├── receipts_list_cashier.html # Чеки касира
│   │   └── ...                   # Інші шаблони
│   │
│   ├── static/css/               # CSS-стилі
│   │   └── theme.css
│   │
│   └── templatetags/             # Власні теги шаблонів
│       └── custom_filters.py     # Користувацькі фільтри
│
├── media/                        # Завантажені файли
│   ├── products/                 # Зображення товарів
│   └── categories/               # Зображення категорій
│
└── logs/                         # Логи системи
```

---

## 📈 Основні можливості системи

### 1. **Транзакційна цілісність**
- Всі операції з фінансами виконуються в транзакціях
- При помилці — автоматичний rollback
- Гарантія консистентності даних

### 2. **Автоматичний облік залишків**
- При продажу — автоматичне зменшення кількості
- При поставці — автоматичне збільшення
- При списанні — зменшення з коментарем
- При поверненні — повернення на склад

### 3. **Розрахунок прибутковості**
- На рівні товару: margin = price - purchase_price
- На рівні чека: сума всіх прибутків позицій
- Загальна статистика на дашборді

### 4. **Валідація даних**
- Ціна продажу ≥ ціна закупівлі
- Кількість товару ≥ 0
- Неможливо продати більше, ніж є на складі
- Перевірка термінів придатності

### 5. **Генерація звітів**
- PDF-чеки з деталізацією
- Експорт даних для аналізу
- Статистика за періодами

---

## 🔒 Безпека

- Всі паролі хешуються (Django Auth)
- CSRF-захист на всіх формах
- Розмежування прав доступу через групи
- SQL-ін'єкції запобігаються ORM
- Валідація даних на рівні моделей та форм

---

## 🐛 Логування

Система логує всі критичні операції:
- Створення/редагування товарів
- Оформлення продажів
- Списання та повернення
- Помилки та виключення

Логи зберігаються у папці `logs/`
